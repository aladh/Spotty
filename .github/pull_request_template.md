Describe the concrete problem and resulting behavior. Link the issues this change resolves.

Validation performed:

For behavior changes, complete these declarations using
`Tests/BrowsingHarness/Scenarios/manifest.json`:

- Product contracts affected:
- Representative scenario IDs run (with revision-matched evidence):
- Scenario IDs added or changed:
- Unverified acceptance scope and reasons:

For changes outside the corpus, explain the scope and relevant verification. State-boundary
scenario execution does not establish visual fidelity or signed Demo network isolation.
