name: Thermos review

on:
  pull_request:
    types: [opened, reopened, ready_for_review, synchronize]
  issue_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      pr_number:
        description: PR number to review again in full (posts another review)
        required: true
        type: string

permissions:
  contents: read

concurrency:
  # Ineligible comments get their own group so they cannot cancel or evict a real review.
  group: >-
    thermos-review-${{
      ((github.event_name == 'workflow_dispatch' &&
        github.ref == format('refs/heads/{0}', github.event.repository.default_branch)) ||
       (github.event_name == 'pull_request' &&
        github.event.pull_request.draft == false &&
        github.event.pull_request.head.repo.full_name == github.repository) ||
       (github.event_name == 'issue_comment' &&
        github.event.issue.pull_request != null &&
        contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.comment.author_association) &&
        contains(github.event.comment.body, '@thermos review')))
      && format('pr-{0}', inputs.pr_number || github.event.pull_request.number || github.event.issue.number)
      || format('ignored-{0}', github.run_id)
    }}
  cancel-in-progress: true

jobs:
  thermos-review:
    if: >-
      (github.event_name == 'workflow_dispatch' &&
       github.ref == format('refs/heads/{0}', github.event.repository.default_branch)) ||
      (github.event_name == 'pull_request' &&
       github.event.pull_request.draft == false &&
       github.event.pull_request.head.repo.full_name == github.repository) ||
      (github.event_name == 'issue_comment' &&
       github.event.issue.pull_request != null &&
       contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.comment.author_association) &&
       contains(github.event.comment.body, '@thermos review'))
    permissions:
      contents: read
      pull-requests: read
      actions: read
      id-token: write
    uses: ./.github/workflows/agent-review.yml
    with:
      pr_number: ${{ format('{0}', inputs.pr_number || github.event.pull_request.number || github.event.issue.number) }}
      rerun_command: '@thermos review'
      reviewer_name: Thermos review
      review_marker: '<!-- spotty-thermos-review -->'
      config_directory: .github/thermos-review
      primary_agent: thermos
      expected_auditors: '2'
      legacy_unmarked_threads: 'true'
      can_approve: 'true'
