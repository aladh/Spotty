[package]
name = "librespot-connect"
version = "0.8.0"
rust-version = "1.85"
authors = ["Paul Lietar <paul@lietar.net>"]
license = "MIT"
description = "The Spotify Connect logic for librespot"
repository = "https://github.com/librespot-org/librespot"
edition = "2024"

[features]
# Refer to the workspace Cargo.toml for the list of features
default = ["native-tls"]
# Used only by Spotty's dev-dependency to drive the real handler offline.
spotty-test-harness = []

# TLS backend propagation
native-tls = ["librespot-core/native-tls"]
rustls-tls-native-roots = ["librespot-core/rustls-tls-native-roots"]
rustls-tls-webpki-roots = ["librespot-core/rustls-tls-webpki-roots"]

[dependencies]
librespot-core = { version = "0.8.0", git = "https://github.com/librespot-org/librespot", rev = "939dc5ee9d833e1980f9495241219d9d4868a061", default-features = false }
librespot-playback = { version = "0.8.0", git = "https://github.com/librespot-org/librespot", rev = "939dc5ee9d833e1980f9495241219d9d4868a061", default-features = false }
librespot-protocol = { version = "0.8.0", git = "https://github.com/librespot-org/librespot", rev = "939dc5ee9d833e1980f9495241219d9d4868a061", default-features = false }

futures-util = { version = "0.3", default-features = false, features = ["std"] }
log = "0.4"
protobuf = "3.7"
rand = { version = "0.9", default-features = false, features = ["small_rng"] }
serde_json = "1.0"
thiserror = "2"
tokio = { version = "1", features = ["macros", "sync"] }
tokio-stream = { version = "0.1", default-features = false }
uuid = { version = "1.18", default-features = false, features = ["v4"] }

[lints.clippy]
redundant_closure_for_method_calls = "warn"
