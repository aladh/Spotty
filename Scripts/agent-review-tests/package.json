{
  "name": "spotty-agent-review-tests",
  "private": true,
  "type": "module",
  "scripts": { "test": "node --test && python3 -m unittest discover -s . -p '*_test.py'" },
  "devDependencies": { "@actions/expressions": "0.3.61" }
}
