id: rust-playing-store-required
valid:
  - |-
    fn apply_player_event_locked(event: PlayerEvent) {
        match event {
            PlayerEvent::Playing { .. } => { IS_PLAYING.store(true, Ordering::SeqCst); }
            _ => {}
        }
    }
  - |-
    #[cfg(test)] mod tests {}
    fn apply_player_event_locked(event: PlayerEvent) {
        match event {
            PlayerEvent::Playing { track_id, .. } => {
                // after test module
                IS_PLAYING.store(true, Ordering::SeqCst);
            }
            _ => {}
        }
    }
  - 'fn apply_player_event_locked(event: PlayerEvent) { match event { crate::PlayerEvent::Playing { .. } => { IS_PLAYING.store(true, Ordering::SeqCst); }, _ => {} } }'
  - 'fn apply_player_event_locked(event: PlayerEvent) { match event { self::PlayerEvent::Playing { .. } => { IS_PLAYING.store(true, Ordering::SeqCst); }, _ => {} } }'
  - 'fn apply_player_event_locked(event: PlayerEvent) { match event { PlayerEvent::Playing { .. } => IS_PLAYING.store(true, Ordering::SeqCst), _ => {} } }'
  - 'fn apply_player_event_locked(event: PlayerEvent) { match event { PlayerEvent::Playing { .. } => { IS_PLAYING.store((true as bool), Ordering::SeqCst); }, _ => {} } }'
invalid:
  - 'fn apply_player_event_locked() {}'
  - |-
    fn apply_player_event_locked(event: PlayerEvent) {
        match event {
            PlayerEvent::Playing { .. } => { IS_PLAYING.store(false, Ordering::SeqCst); }
            _ => { IS_PLAYING.store(true, Ordering::SeqCst); }
        }
    }
  - |-
    fn apply_player_event_locked(event: PlayerEvent) {
        match event {
            PlayerEvent::Playing { .. } => {
                // IS_PLAYING.store(true, Ordering::SeqCst);
            }
            _ => {}
        }
    }
  - |-
    #[cfg(test)] mod tests {
        fn apply_player_event_locked(event: PlayerEvent) {
            match event {
                PlayerEvent::Playing { .. } => { IS_PLAYING.store(true, Ordering::SeqCst); }
                _ => {}
            }
        }
    }
  - 'fn apply_player_event_locked(event: PlayerEvent) { match event { PlayerEvent::Playing { .. } => {}, _ => {} } } fn decoy(event: PlayerEvent) { match event { PlayerEvent::Playing { .. } => { IS_PLAYING.store(true, Ordering::SeqCst); }, _ => {} } }'
  - 'fn apply_player_event_locked(event: PlayerEvent) { match event { PlayerEvent::Playing { .. } => { #[cfg(test)] mod tests { fn stub() { IS_PLAYING.store(true, Ordering::SeqCst); } } }, _ => {} } }'
  - 'fn apply_player_event_locked(event: PlayerEvent) { match event { PlayerEvent::Playing { .. } => { fn decoy(event: PlayerEvent) { match event { PlayerEvent::Playing { .. } => { IS_PLAYING.store(true, Ordering::SeqCst); }, _ => {} } } }, _ => {} } }'
