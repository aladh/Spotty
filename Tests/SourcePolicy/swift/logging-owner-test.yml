---
id: logging-owner
valid:
- SpottyLog.playback.info("Request accepted")
- debugLog("Engine", "Request failed")
- let fingerprint = value.fingerprint
- "// print(secret)"
- 'let text = "Logger(subsystem: x)"'
- import OSLog
invalid:
- print(token)
- Swift.print(token)
- 'let log = Logger(subsystem: "app", category: "auth")'
- 'let log = os.Logger.init(subsystem: "app", category: "auth")'
- NSLog("%s", payload)
- fputs(message, stderr)
- let output = debugPrint
